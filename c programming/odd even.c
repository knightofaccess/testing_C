#include<stdio.h>
int main()
{
    int a,b,x,sum;
    int num=1;
    b=0;
    sum=0;
    
    while(num<100)
    {
    if(num%2!=0)
    {
    sum = sum + x;
    b++;
}

h   num++;
}
printf("the sum of odd numbers =%d\n",sum);
printf("the nuber of odd numbers =%d\n",b);
getch();
}
